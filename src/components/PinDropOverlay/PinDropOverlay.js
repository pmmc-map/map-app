import React from 'react';
import './style.css';

/**
 * this will show instructions on how to place a pin,
 * then show pin dropping controls
 * TODO: add toggling instructins/pin drop controls
 */
const PinDropOverlay = props => {
	return (
		<div className='pin-drop-instr-background'>
			<h1>drop a pin isntructions</h1>
		</div>
	);
};

export default PinDropOverlay;
